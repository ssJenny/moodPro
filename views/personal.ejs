
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="icon" href="../../favicon.ico">

    <title>Narrow Jumbotron Template for Bootstrap</title>

    <!-- Bootstrap core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

<div class="container">
    <div class="header clearfix personHead">
        <nav>
            <ul class="nav nav-pills pull-right">
                <li role="presentation" class="active"><a href="/">Home</a></li>
                <li role="presentation"><a href="#">About</a></li>
                <li role="presentation"><a href="#">Contact</a></li>
            </ul>
        </nav>
        <h3 class="text-muted"><%= username %></h3>
    </div>



        <% include slider.ejs %>


    <div class="container moodList">
        <% for ( var i = 0; i < comment.length; i++){%>
        <div class="row">
            <div class="col-md-10 col-md-offset-1">
                <h2><a href="/personal:<%= comment[i].username %>"><%= comment[i].username %></a></h2>
                <p><%= comment[i].content %> </p>
                <p class="text-right"><%= comment[i].datetime %></p>

                <% for(var j = 0; j < discuss.length; j++){ %>

                <div class="row">
                    <div class="col-md-12 ">

                        <% if(discuss[j].commentId == comment[i]._id){ %>
                        <p><h4><a href="/<%= discuss[j].username %>"><%= discuss[j].username %></a>:</h4><%= discuss[j].content %> </p>
                        <p class="text-right"><%= discuss[j].datetime %></p>
                        <% } %>

                    </div>
                </div>
                <% } %>

                <% if(isLogin){ %>



                <p ><a href="javascript:void(0)" class="showDisText">评论</a></p>

                <div class="form-group disText" >

                    <textarea class="form-control" rows="3"></textarea>
                    <p style="float: right; margin-top: 5px"><button class="btn btn-primary punishComment" data-commentid="<% comment[i]._id%>" >发表</button></p>
                </div>

                <div class="alert alert-danger" id="<%= comment[i]._id %>"></div>
                <% } %>
            </div>

        </div>



        <% } %>
        <% if(total >6){%>
        <div class="row">
            <div class="col-md-10 col-md-offset-1"><button type="button" class="btn  btn-block " onclick="location='/moodList'">更多>></button></div>
        </div>
        <% } %>
    </div>

    <footer class="footer">
        <p class="text-center">&copy; 2017 Personal, ciyel.</p>
    </footer>

</div> <!-- /container -->

</body>
<script src="js/jquery-1.11.3.min.js"></script>
<script src="js/bootstrap.min.js"></script>
</html>
